# 本地开发专用配置：仅用于本机联调，切勿用于任何生产环境。
# 生产环境请通过环境变量注入 JWT_SECRET / DB_PASSWORD，并使用 prod 配置。
auth:
  jwt:
    secret: ${JWT_SECRET:dev-only-secret-please-override-in-production-32b+}
  sms:
    provider: mock

spring:
  jpa:
    show-sql: false

logging:
  level:
    com.scriptplatform: debug
