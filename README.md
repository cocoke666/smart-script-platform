# 智能剧本创作平台

## 目录结构

```
pc/    PC 管理后台，Vue 3 + Vite + TypeScript
```

## 运行

```bash
cd pc
npm install
npm run dev
```

其他命令：

```bash
npm run build     # 构建（含 TypeScript 类型检查）
npm run preview   # 预览构建产物
```
